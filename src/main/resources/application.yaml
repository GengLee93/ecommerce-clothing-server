spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/shopping_db
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true